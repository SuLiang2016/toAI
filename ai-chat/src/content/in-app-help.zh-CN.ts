import type { InAppHelpSection } from './in-app-help';

export const IN_APP_HELP_TITLE = 'AI Chat 使用说明';

export const IN_APP_HELP_INTRO = [
  '本说明聚焦当前版本已经内置的三类工作流，帮助你直接在应用内完成模型切换、提示词模板管理，以及会话的新建与切换。',
  '以下步骤全部基于现有界面，不会额外引入独立设置页、语言向导或额外浮层。',
].join('\n');

export const IN_APP_HELP_SECTIONS: InAppHelpSection[] = [
  {
    id: 'model-switching',
    title: '模型配置与切换',
    body: [
      '1. 点击右上角 `设置`，打开 `提供商预设` 对话框。',
      '2. 使用 `新增预设` / `编辑预设` 填写或更新提供商名称、API Base URL、模型名称和能力提示。',
      '3. 在预设列表中点击 `激活`，即可让该预设成为当前会话后续请求使用的模型配置。',
      '4. 如需确认配置是否可用，可点击 `检测连通性`。结果会显示在预设面板里，也会同步显示在聊天标题下方的提供商状态栏中。',
      '5. 若删除当前激活的预设，应用会回退到环境默认配置。API Key 仍只从 `.env.local` 读取，不会保存在预设中。',
    ].join('\n'),
  },
  {
    id: 'prompt-templates',
    title: '提示词模板的创建与使用',
    body: [
      '1. 在左侧边栏找到 `模板` 区域，点击 `新增` 创建模板。',
      '2. 标题可选，内容必填。若标题留空，应用会根据内容自动生成一个简短标题。',
      '3. 保存后，模板会保留在边栏列表中。点击模板卡片会把模板内容插入当前草稿；如果草稿里已有文本，会追加而不是清空。',
      '4. 使用模板卡片上的 `编辑` 和 `删除`，即可持续维护模板内容。',
      '5. 模板保存在浏览器本地存储中，刷新页面后依然可用。',
    ].join('\n'),
  },
  {
    id: 'conversation-switching',
    title: '创建与切换会话',
    body: [
      '1. 点击左侧边栏的 `+ 新对话` 开始新会话。标题区域会显示 `新对话`，草稿也会单独保存。',
      '2. 左侧列表按最近更新时间排序。点击任意会话行即可切换，并恢复该会话的消息与草稿。',
      '3. 使用搜索框可按标题、消息内容或附件名称筛选会话，方便快速找回旧线程。',
      '4. `收件箱` 与 `已归档` 可切换列表视图。归档适合暂时隐藏不常用会话；恢复后仍可继续编辑。',
      '5. 将鼠标悬停在会话行上，可执行置顶、归档、重命名或删除。这些操作只影响列表组织方式，不会改写会话历史。',
    ].join('\n'),
  },
];

export const IN_APP_HELP_MARKDOWN = [
  `# ${IN_APP_HELP_TITLE}`,
  IN_APP_HELP_INTRO,
  ...IN_APP_HELP_SECTIONS.flatMap(section => [`## ${section.title}`, section.body]),
].join('\n\n');
